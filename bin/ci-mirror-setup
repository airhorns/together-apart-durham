#!/usr/bin/env bash
set -ex
git remote add outaouais https://${GITHUB_ACTOR}:${INPUT_GITHUB_TOKEN}@github.com/airhorns/together-apart-outaouais.git
git remote add durham https://${GITHUB_ACTOR}:${INPUT_GITHUB_TOKEN}@github.com/airhorns/together-apart-durham.git